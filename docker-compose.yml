services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - SQLITE_DB_PATH=/app/data/survey.db
    volumes:
      - survey_data:/app/data

volumes:
  survey_data:
